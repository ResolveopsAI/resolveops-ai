version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: resolveops-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-resolveopsadmin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-local-db-pass}
      POSTGRES_DB: ${POSTGRES_DB:-resolveopsdb}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-resolveopsadmin}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    container_name: resolveops-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - frontend-net
      - backend-net
    depends_on:
      - frontend
      - api-gateway-service

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE_URL=/api
    networks:
      - default
      - frontend-net
    depends_on:
      - api-gateway-service
    restart: unless-stopped

  api-gateway-service:
    build:
      context: ./services/api-gateway-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - generated_visuals:/app/data/visuals
    networks:
      - default
      - frontend-net
      - backend-net
    depends_on:
      postgres:
        condition: service_healthy
      ai-rca-service:
        condition: service_started
    environment:
      - VISUAL_STORAGE_DIR=/app/data/visuals
      - SSH_CREDENTIAL_SERVICE_URL=http://ssh-credential-service:8000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  auth-service:
    build:
      context: ./services/auth-service
    env_file:
      - .env
    networks:
      - backend-net
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  github-intelligence-service:
    build:
      context: ./services/github-intelligence-service
    env_file:
      - .env
    networks:
      - backend-net
    restart: unless-stopped

  aws-intelligence-service:
    build:
      context: ./services/aws-intelligence-service
    env_file:
      - .env
    networks:
      - backend-net
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  azure-intelligence-service:
    build:
      context: ./services/azure-intelligence-service
    env_file:
      - .env
    networks:
      - backend-net
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  mcp-server-service:
    build:
      context: ./services/mcp-server-service
    env_file:
      - .env
    networks:
      - backend-net
    depends_on:
      - aws-intelligence-service
      - github-intelligence-service
      - azure-intelligence-service
      - docker-evidence-adapter
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  docker-evidence-adapter:
    build:
      context: ./services/docker-evidence-adapter
    expose:
      - "8000"
    env_file:
      - .env
    networks:
      - backend-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  docker-operations-service:
    build:
      context: ./services/docker-operations-service
    expose:
      - "8000"
    env_file:
      - .env
    networks:
      - backend-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  ai-rca-service:
    build:
      context: ./services/ai-rca-service
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8005:8000"
    env_file:
      - .env
    environment:
      - VISUAL_STORAGE_DIR=/app/data/visuals
      - OPENAI_MODEL=${OPENAI_MODEL_NAME:-gpt-4o-mini}
    volumes:
      - generated_visuals:/app/data/visuals
    networks:
      - backend-net
    depends_on:
      - mcp-server-service
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
    restart: unless-stopped

  notification-service:
    build:
      context: ./services/notification-service
    env_file:
      - .env
    networks:
      - backend-net
    restart: unless-stopped

  ssh-credential-service:
    build:
      context: ./services/ssh-credential-service
    ports:
      - "8007:8000"
    env_file:
      - .env
    environment:
      - SSH_CREDENTIAL_ENCRYPTION_KEY=${SSH_CREDENTIAL_ENCRYPTION_KEY:-dev-encryption-key-change-in-prod}
      - AI_RCA_SERVICE_URL=http://ai-rca-service:8000
    networks:
      - backend-net
    restart: unless-stopped

networks:
  frontend-net:
    driver: bridge
  backend-net:
    driver: bridge

volumes:
  postgres_data:
  generated_visuals:
  caddy_data:
  caddy_config:
